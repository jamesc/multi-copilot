# Auto-detect text files and normalize line endings
* text=auto

# Shell scripts must use LF (Unix) line endings
*.sh text eol=lf

# PowerShell can use either, but prefer CRLF on Windows
*.ps1 text eol=crlf

# Keep these as-is
*.png binary
*.jpg binary
*.gif binary
